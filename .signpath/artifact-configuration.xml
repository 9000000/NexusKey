<?xml version="1.0" encoding="utf-8" ?>
<artifact-configuration xmlns="http://signpath.io/artifact-configuration/v1">
  <!-- Install this configuration in SignPath with slug
       "vkeyclassic-foundation". Each approved file is mandatory and must match
       exactly once. The dedicated workflow rejects every additional file before
       uploading this ZIP. -->
  <zip-file>
    <pe-file path="VKeyClassic.exe">
      <authenticode-sign />
    </pe-file>
    <pe-file path="VKeyTSF.dll">
      <authenticode-sign />
    </pe-file>
    <pe-file path="VKeyWatchdog.exe">
      <authenticode-sign />
    </pe-file>
  </zip-file>
</artifact-configuration>
